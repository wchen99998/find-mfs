[build-system]
requires = ["maturin>=1.9,<2"]
build-backend = "maturin"

[project]
name = "find-mfs-rust"
version = "0.1.0"
requires-python = ">=3.10,<3.14"
license = {text = "GPL-3.0-or-later"}

[tool.maturin]
bindings = "pyo3"
module-name = "find_mfs._rust"
python-source = "../.."
